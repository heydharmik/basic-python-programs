

#  Tuples are immutable i.e. tuples can't be changed
coordinates = (4, 5)
#  coordinates = [(4, 5), (6,7), (8, 9)]  #  List which is immutable
#  coordinates = [3,5]  #  This creates list
#  coordinates[1] = 10  #  This will throw an error
print(coordinates[0])
print(coordinates[1])